# Mnemo — Demo Video Storyboard (<3 min)

> **Category:** Production ML Agents · **Scored under:** Submission Quality (judges' first impression).
> **HERO:** silent upstream semantic re-point caught by memory — the exact arc `run_ml_drift_demo.py` prints live.
> **Honesty contract (do NOT narrate past this):** the demos really run; "wakes on event" is a **polling** reconcile loop
> (say "Mnemo's reconcile pass runs", not "fires on the event"); insight/summary text is generated by **local Ollama**;
> the **eval harness is BUILT & run** (WITHOUT 0.53 / WITH 1.00 / PLACEBO 0.33, lift +0.467) — Shot 10 shows the REAL
> `examples/eval_lift.svg`, never a fabricated bar; note on camera that WITH is a near-ceiling (see `examples/EVAL_NOTES.md`).
> **Recording kit:** one terminal (720p+, large font) + Chrome on DataHub UI `http://localhost:9002`. No editing beyond cuts + the voiceover track. Recordable in an afternoon.

---

## THE OPENING HOOK — 0:00–0:15 (voiceover, verbatim)

> **"This is how a production model dies. Not with an error — with silence.
> Someone re-points an upstream table. Same column names. Same feature. No alert fires.
> The model just quietly starts scoring on the wrong data — and nobody notices until the business does."**

On screen while this is spoken: **Shot 1** below.

---

## Shot list

| # | Time | On-screen (terminal / DataHub UI :9002) | Voiceover (tight) | What it proves — the moat |
|---|------|-----------------------------------------|-------------------|---------------------------|
| **1** | 0:00–0:15 | **Cold open — the pain.** DataHub UI on the `churn_model` (mlflow, PROD) entity page, calm and green. Slowly zoom on the "PROD" badge. Nothing looks wrong. | *(the verbatim hook above)* | Sets the stakes: the failure is **invisible to the current UI** — a valid model, a valid feature, no error. |
| **2** | 0:15–0:35 | **Established memory (conf 0.90).** Split: terminal running `python run_ml_drift_demo.py`, `BEAT 1` output showing `model memory established: confidence 0.901, remembered sources ['urn:...fct_users_created,PROD)']`. Cut to DataHub UI → `churn_model` → Properties tab → the `mnemo.*` structured properties: `mnemo.confidence 0.901`, summary "inputs healthy: days_since_signup ← fct_users_created". | "Mnemo has already watched this model's inputs across events. It doesn't re-derive from scratch — it **remembers**: a Bayesian confidence of 0.90, and the exact source its feature is fed from, written **on the graph itself**." | **Per-asset memory that lives ON the graph** with principled confidence + provenance. The reference (conversation-history in a local DB) has nothing on the graph. |
| **3** | 0:35–1:00 | **The silent re-point.** Terminal `BEAT 2`: `feature 'days_since_signup' silently re-pointed fct_users_created → fct_users_created_v2` / `(name unchanged, description unchanged — a schema-diff sees nothing)`. Cut to DataHub UI: the two source datasets side by side — `signup_ts` vs `ingest_ts` — but the **feature name and description are identical**. Optionally highlight both `timestamp` columns to show a column-diff would match. | "A data engineer swaps the feature's upstream. The new table's timestamp means **ingest time**, not **signup time**. Same feature name. Same description. No error thrown. Every one-shot schema-diff sees a valid model — this is exactly how models die silently." | **The name-blind blind spot.** A schema/column-diff inspects the current state and sees nothing changed in name or type. This is why the catch *requires* remembered prior state. |
| **4** | 1:00–1:20 | **Mnemo compares to memory.** Terminal `BEAT 3-4`: `remembered: [...fct_users_created...]` / `now: [...fct_users_created_v2...]` / `source delta detected: True`. Let the two URN lines sit on screen a beat — the diff is the whole story. | "Mnemo's reconcile pass runs, loads the source it **remembered**, re-reads the feature's live lineage — and sees the delta. Not a broken schema. A source that changed **relative to what it knew before**." | **Delta under a stable schema** — detectable only with remembered graph state + live lineage read. This is the real, built signal (source-set diff), not an LLM guess. |
| **5** | 1:20–1:45 | **Re-score → governed Proposal.** Terminal: `confidence 0.901 → 0.600` then `⚠️ below governance threshold (0.7) → OPEN DATAHUB PROPOSAL (no silent auto-trust)`. Cut to DataHub UI: a Proposal / warning card on `churn_model` — "Upstream semantic-drift risk: days_since_signup source changed fct_users_created → fct_users_created_v2; inputs may be silently corrupted. confidence 0.60." | "The contradicting evidence drops confidence from 0.90 to 0.60 — below the governance line. And because this is a **PROD** model, Mnemo doesn't auto-write an alarm. It opens a **DataHub Proposal** — a human gate — with the evidence attached." | **Bayesian re-score + governance-native gating.** Low-confidence finding on a high-tier asset routes to a Proposal, not a silent mutation. The reference does neither. |
| **6** | 1:45–2:05 | **Payoff + the kill.** DataHub UI: reflection/summary card (text from local Ollama) — "No single asset said this. days_since_signup is fine alone; fct_users_created_v2 is fine alone. Only by remembering that this feature fed a PROD model from a specific source did the risk surface — before the next training run baked it in." | "Caught before the next training run baked the drift into production. **The reference agent suggests once; Mnemo remembers — and catches what a schema-diff can't.**" | The one-line kill. The moat = **model-lineage memory that compounds across events**; the catch structurally needs remembered state + lineage traversal + governed write. |
| **7** | 2:05–2:20 | **Architecture beat (15s).** Show the ASCII diagram from `ARCHITECTURE.md §4` (or a clean redraw): DataHub GMS/UI/Kafka → Mnemo (reconcile → Bayesian confidence → governance gate → reflect) → typed structured properties / Proposal. Point at the reconcile loop as it's named. | "Under the hood: Mnemo reads the graph through DataHub's APIs, reconciles its prior memory with a Bayesian confidence model, gates every write on governance signals, and writes typed structured properties back — a polling reconcile loop today, event-driven next." | Grounds the pitch in real, verified plumbing (MCP read, `confidence_model.py`, structured-property write). Honest about polling. |
| **8** | 2:20–2:35 | **Honesty + close.** Terminal final lines: `ML-DRIFT DEMO GREEN ✅` and the `[honesty]` block — "REAL = the source-delta detection… the confidence MAGNITUDE is set by Bayesian priors, not a measured drift score." Then a title card: **Mnemo — compounding, governed memory for the data graph.** | "The source-delta detection is real and live-verified. The confidence math is a principled Bayesian prior, not a fake drift score — and we say so. Mnemo: memory that compounds, governed at the graph." | Rigor signal for the OpenAI-TPM / governance judges — the entry that **states what's real** reads as the credible one. |

**Total runtime: ~2:35 (8 shots).** Comfortable margin under 3:00.

---

## Optional (only if actually built — otherwise CUT, do not fabricate)

| # | Time | On-screen | Voiceover | Note |
|---|------|-----------|-----------|------|
| **9** | +0:00–0:15 | **Reflection crown.** `run_reflection_demo.py` output: lineage-wide insight, `confidence 0.912`, citing 3 assets, written on the model. | "And it goes further — Mnemo reflects across the whole lineage path to a graph-level insight that lives on no single asset." | `mnemo/reflection.py` is BUILT & GREEN (§10). Safe to include; drop Shot 8's tail to hold under 3:00 if added. |
| **10** | +0:00–0:15 | Eval lift bar (`examples/eval_lift.svg`): WITHOUT 0.53 → **WITH_RAW 0.93** → WITH 1.00, PLACEBO 0.33. | "Measured: stripped to bare facts, the memory lifts triage accuracy from 0.53 to 0.93 by reasoning — and irrelevant memory (placebo) *hurts*, so it's the relevant memory, not just more tokens." | **BUILT & run (§10)** — real chart from `eval/results.csv`. Lead with WITH_RAW 0.93 (the honest, production-realistic number); WITH=1.00 is the acknowledged ceiling; PLACEBO 0.33 is the rigor control. |

---

## Recording order (efficient for a solo builder)
1. Reset DataHub, run `python run_ml_drift_demo.py` once end-to-end — screen-record the **whole terminal** (covers Shots 2,3,4,5,8). Do a second clean take for safety.
2. In Chrome on `:9002`, capture the `churn_model` page states: green (Shot 1), `mnemo.*` properties (Shot 2), two source schemas side by side (Shot 3), the Proposal/warning card + reflection card (Shots 5,6). Take these AFTER the run so the graph is populated.
3. Screenshot the `ARCHITECTURE.md §4` diagram for Shot 7.
4. Record the voiceover last against the assembled cut; the hook (0:00–0:15) is verbatim above.
